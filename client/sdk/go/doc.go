// Package sdk is the in-process Go SDK form: the client core exposed as a
// library (the core *without* the server shell). create() verifies + seals
// inline — lowest latency, no extra process.
package sdk
