package wire_test

// e2eeKeyForTest mirrors the package-internal reserved key name, so a test can
// rewrite a frame the way a non-conforming sealer would have emitted it.
const e2eeKeyForTest = "_e2ee"
